# zx/functional

Composable function pipelines and higher-order utilities. Provides a `pipe`/`pipeline` abstraction, a non-owning callable reference, `let` binding, and small combinators like `do_all`, `apply`, and `with`.
