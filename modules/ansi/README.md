# zx/ansi

ANSI escape code types and a 2D terminal surface (screen buffer) with a renderer. Models terminal colors, font attributes, styled glyphs, and converts a surface to an escape-code string for display.

**CMake target:** `ansi`
**Dependencies:** `arrays`, `mat`, `core`
